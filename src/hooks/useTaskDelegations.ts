// src/hooks/useTaskDelegations.ts

// Deixamos este hook como um placeholder para funcionalidades futuras.
export const useTaskDelegations = () => {
  return {}; // Retorna um objeto vazio por enquanto.
};