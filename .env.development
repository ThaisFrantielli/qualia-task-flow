# Enable Vite websocket HMR in development
VITE_ENABLE_WEBSOCKET=true
# Optional: prefer this dev port. If 8080 is free, Vite will use this; fallback logic still applies.
VITE_DEV_SERVER_PORT=8081
VITE_FORCE_WSS=false
